spring.application.name=edulib-backend

# MySQL
spring.datasource.url=jdbc:mysql://localhost:3306/edulib_db?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=chinni@1234
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# JWT
app.jwt.secret=change_this_to_a_very_long_random_secret_key_for_jwt_signing_2026
app.jwt.expiration-ms=86400000

app.upload.dir=uploads

# CORS (frontend + local dev)
app.cors.allowed-origins=https://your-frontend.vercel.app,http://localhost:3000

server.port=8080
